# Frontend (config_store admin UI)

Web admin for managing config_store entries.

## Status

Placeholder. To be scaffolded with Next.js + shadcn/ui.

## Dev (once scaffolded)

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to point at the backend, e.g. `http://localhost:6002`.

## Build

```bash
docker build -t config_store_frontend ./frontend
```
